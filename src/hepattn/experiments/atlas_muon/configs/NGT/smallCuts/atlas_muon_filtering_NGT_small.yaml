seed_everything: 42
name: ATLAS-Muon-small

# ckpt_path: /shared/tracking/hepattn_muon/src/logs/ATLAS-Muon-small-NSWimpact_20250923-T115813/ckpts/epoch=022-val_loss=0.01176.ckpt
data: 
  train_dir: /scratch/ml_training_data_2694000_hdf5
  val_dir: /scratch/ml_validation_data_144000_hdf5

  num_workers: 100
  num_train: -1
  num_val: -1
  num_test: -1
  batch_size: 50  # Start with batch_size=1 to avoid smart batching issues
  # use_smart_batching: false  # Disable smart batching as requested

  # ATLAS muon data parameters
  event_max_num_particles: 10  # Typically fewer tracks per event in muon data

  # Define which inputs will be available to the model
  inputs:
    hit:
      # Global hit coordinates
      - spacePoint_globEdgeHighX
      - spacePoint_globEdgeHighY
      - spacePoint_globEdgeHighZ
      - spacePoint_globEdgeLowX
      - spacePoint_globEdgeLowY
      - spacePoint_globEdgeLowZ
      - spacePoint_time
      - spacePoint_driftR
      # Add covariance information
      - spacePoint_covXX
      - spacePoint_covXY
      - spacePoint_covYX
      - spacePoint_covYY
      # Add detector information
      - spacePoint_channel
      - spacePoint_layer
      - spacePoint_stationPhi
      - spacePoint_stationEta
      - spacePoint_stationIndex
      - spacePoint_technology
      # Add derived hit fields
      - r  # Radial distance
      - s  # 3D distance
      - theta  # Polar angle
      - phi  # Azimuthal angle
  targets:
    hit:
      - on_valid_particle
    # particle:
    #   - truthMuon_pt
    #   - truthMuon_eta
    #   - truthMuon_phi
    #   - truthMuon_q

trainer:
  # Training configuration
  max_epochs: 50
  num_sanity_val_steps: 1
  accelerator: gpu
  devices: [1]
  # devices: -1
  # devices: [1,2,3,4,5,6,7]
  # devices: -1
  precision: bf16-mixed
  log_every_n_steps: 50
  default_root_dir: logs
  # TODO: Try a looser gradient clipping like 0.1 some time to see if it helps with performance
  # gradient_clip_val: 1.0
  gradient_clip_val: 0.1
  enable_progress_bar: True

  # Specify loggers here
  logger:
    class_path: hepattn.utils.loggers.CometLogger
    init_args:
      project_name: atlas_muon_filtering

  callbacks:
    - class_path: hepattn.callbacks.Compile
    - class_path: hepattn.callbacks.InferenceTimer
    - class_path: hepattn.callbacks.SaveConfig
    - class_path: hepattn.callbacks.Checkpoint
      init_args:
        monitor: val/loss
        mode: min
        save_top_k: 5
    - class_path: hepattn.callbacks.PredictionWriter
      init_args:
        write_inputs: false
        write_outputs: false
        write_preds: True
        write_targets: false
        write_losses: false
    - class_path: lightning.pytorch.callbacks.LearningRateMonitor
    - class_path: lightning.pytorch.callbacks.TQDMProgressBar
      init_args:
        refresh_rate: 50
    - class_path: lightning.pytorch.callbacks.EarlyStopping
      init_args:
        monitor: val/reconstructable_particle_retention
        patience: 5 
        min_delta: 0.0
        mode: max
        verbose: True 

model:
  optimizer: Lion

  lrs_config:
    initial: 1e-5
    max: 5e-5
    end: 1e-6
    pct_start: 0.05
    weight_decay: 1e-5

  model:
    class_path: hepattn.models.HitFilter
    init_args:
      input_sort_field: phi
      input_nets:
        class_path: torch.nn.ModuleList
        init_args:
          modules:
            - class_path: hepattn.models.InputNet
              init_args:
                input_name: hit
                fields:
                  # Global hit coordinates
                  - spacePoint_globEdgeHighX
                  - spacePoint_globEdgeHighY
                  - spacePoint_globEdgeHighZ
                  - spacePoint_globEdgeLowX
                  - spacePoint_globEdgeLowY
                  - spacePoint_globEdgeLowZ
                  - spacePoint_time
                  - spacePoint_driftR
                  # Add covariance information
                  - spacePoint_covXX
                  - spacePoint_covXY
                  - spacePoint_covYX
                  - spacePoint_covYY
                  # Add detector information
                  - spacePoint_channel
                  - spacePoint_layer
                  - spacePoint_stationPhi
                  - spacePoint_stationEta
                  - spacePoint_technology
                  - spacePoint_stationIndex
                  # Add derived hit fields
                  - r  # Radial distance
                  - s  # 3D distance
                  - theta  # Polar angle
                  - phi  # Azimuthal angle
                net:
                  class_path: hepattn.models.Dense
                  init_args:
                    input_size: 22  # Number of input features for ATLAS muon data
                    output_size: &dim 128
                    norm_input: true
                posenc:
                  class_path: hepattn.models.posenc.PositionEncoder
                  init_args:
                    input_name: hit
                    dim: *dim
                    fields:
                      - r
                      - theta
                      - phi
                    dim: *dim
                    # scale: 0.1
      encoder:
        class_path: hepattn.models.Encoder
        init_args:
          num_layers: 8 # Slightly fewer layers for muon data
          dim: *dim
          attn_type: flash
          window_size: 512  # Smaller window size for fewer hits per event
          window_wrap: true
          hybrid_norm: true
          norm: RMSNorm
          attn_kwargs:
            num_heads: 8

      tasks:
        class_path: torch.nn.ModuleList
        init_args:
          modules:
            - class_path: hepattn.models.task.HitFilterTask
              init_args:
                name: hit_filter
                input_object: hit
                target_field: on_valid_particle
                dim: *dim
                threshold: 0.01
                loss_fn: bce
